apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  labels:
    app: ibm-operator-catalog
  name: argoCD
  namespace: rhacm-def
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  selector:
    matchLabels:
      app: argoCD
